# AgileFusionChallenge

## Gist
Web App for monitoring system resources: processes, memory and CPU load.
For notification web sockets are used, since challenge forbids using 3rd party libraries, e.g. SignalR
